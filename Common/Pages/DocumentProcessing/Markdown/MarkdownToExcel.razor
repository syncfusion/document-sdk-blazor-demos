@page "/markdown/markdown-to-excel"
@inject Microsoft.JSInterop.IJSRuntime JS
@inject HttpClient Http
@using System.IO;
@using Syncfusion.Blazor.Buttons
@*Hidden:Lines*@
@using BlazorDemos.Data.FileFormats.Markdown
@*End:Hidden*@
@*Hidden:Lines*@
@inherits SampleBaseComponent
@inject NavigationManager NavigationManager
@*End:Hidden*@

<SampleDescription>
    <p>This sample demonstrates how to convert Markdown documents to Excel documents using .NET Excel (XlsIO) and .NET Markdown libraries.</p>
</SampleDescription>
<ActionDescription>
    <p style="font-size:16px">The .NET Excel (XlsIO) library provides support to convert Markdown documents to Excel format.</p><p style="font-size:16px"><ul style="font-size:16px"><li>Supports conversion of Markdown documents to Excel format, including text, numbers, formulas, and number formatting.</li></ul></p>
    <p style="font-size:16px">More information about Markdown to Excel conversion can be found in this <a href="https://help.syncfusion.com/document-processing/excel/conversions/markdown-to-excel/overview" aria-label="documentation section">documentation</a> section.</p>
</ActionDescription>

<div class="control-section">
    <p style="font-weight:normal">Click the button to view the Excel document generated using the .NET Excel (XlsIO) and .NET Markdown libraries. Please note that Microsoft Excel Viewer or Microsoft Excel is required to view the Excel document.</p>
    <div class="button-section">
        <div id="button-control">
            <div class="row">
                <div>
                    <SfButton @onclick="InputTemplate">View Template</SfButton>&nbsp;
                    <SfButton @onclick="MarkdownConversion">Convert To Excel</SfButton>
                </div>
            </div>
        </div>
    </div>
</div>

<style>   
    .control-section .row {
        margin: 10px 0;
    }
    .e-radio-wrapper {
    width:250px;
    margin-right: 20px;
    }
</style>

@code {
    MemoryStream excelStream;
    //Hidden:Lines 
    Dictionary<string, MemoryStream> fileData = new Dictionary<string, MemoryStream>();   
    //End:Hidden

    /// <summary>
    /// Create and download the simple Excel document
    /// </summary>
    public async Task MarkdownConversion()
    {
        string fileDataPath = NavigationManager.BaseUri + SampleService.WebAssetsPath;
        byte[] data = await Http.GetByteArrayAsync(fileDataPath + "data/markdown/markdown-to-excel.md");
        fileData.Add("markdown-to-excel.md", new MemoryStream(data));
        MarkdownToExcelService service = new MarkdownToExcelService(fileData);
        excelStream = service.MarkdownToExcel("Markdown");
        service.Close();
        await JS.SaveAs("MarkdownToExcel.xlsx", excelStream.ToArray());
    }
    /// <summary>
    /// Create and download the simple Excel document
    /// </summary>
    public async Task InputTemplate()
    {
        string fileDataPath = NavigationManager.BaseUri + SampleService.WebAssetsPath;
        byte[] data = await Http.GetByteArrayAsync(fileDataPath + "data/markdown/markdown-to-excel.md");
        fileData.Add("markdown-to-excel.md", new MemoryStream(data));
        MarkdownToExcelService service = new MarkdownToExcelService(fileData);
        excelStream = service.MarkdownToExcel("View Template");
        service.Close();
        await JS.SaveAs("InputTemplate.md", excelStream.ToArray());
    }
}
